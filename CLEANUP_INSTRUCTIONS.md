# Подробная инструкция по очистке проекта

## Содержание

1. [Введение](#введение)
2. [Варианты очистки](#варианты-очистки)
3. [Автоматическая очистка](#автоматическая-очистка)
4. [Ручная очистка](#ручная-очистка)
5. [Частичная очистка](#частичная-очистка)
6. [Создание резервной копии](#создание-резервной-копии)
7. [Проверка результатов](#проверка-результатов)
8. [FAQ](#faq)

---

## Введение

После завершения работы с проектом и защиты диплома вы можете освободить значительное место на диске, удалив временные файлы, Docker образы и сам проект.

**Что будет удалено:**
- Docker контейнеры и образы (~500-600 MB)
- Python виртуальное окружение (~500 MB)
- Обученная ML модель (~10 MB)
- База данных результатов (~5 MB)
- Временные файлы и кэш (~50 MB)
- Исходный код проекта (~5 MB)

**Итого освободится:** ~1-1.2 GB

---

## Варианты очистки

### 1. Автоматическая полная очистка (рекомендуется)

Интерактивный скрипт с подтверждениями на каждом шаге.

```bash
cd /Users/kirillzaitsev/Documents/diplom
./cleanup.sh
```

**Преимущества:**
- Безопасно (запрашивает подтверждение)
- Опция создания резервной копии
- Пошаговое выполнение
- Цветной вывод для наглядности

### 2. Легкая очистка (сохранить код)

Удаляет только временные файлы, код остается.

```bash
cd /Users/kirillzaitsev/Documents/diplom
./cleanup-light.sh
```

**Освобождается:** ~500-600 MB
**Сохраняется:** Исходный код, документация, датасет

### 3. Ручная очистка

Пошаговое выполнение команд вручную (для опытных пользователей).

---

## Автоматическая очистка

### Шаг 1: Запуск скрипта

```bash
cd /Users/kirillzaitsev/Documents/diplom
chmod +x cleanup.sh
./cleanup.sh
```

### Шаг 2: Ответы на вопросы

Скрипт задаст следующие вопросы:

1. **Создать резервную копию проекта?** (y/n)
   - Рекомендуется ответить `y`, если проект может понадобиться в будущем
   - Архив будет сохранен на рабочем столе

2. **Остановить Docker контейнеры?** (y/n)
   - Ответьте `y` для остановки всех запущенных контейнеров

3. **Удалить Docker образы?** (y/n)
   - Ответьте `y` для освобождения ~500-600 MB

4. **Очистить временные файлы и кэш?** (y/n)
   - Ответьте `y` для удаления Python/Go кэша, логов, БД

5. **Удалить весь проект полностью?** (y/n)
   - ⚠️ **ВНИМАНИЕ**: Это удалит всё, включая исходный код
   - Ответьте `y` только если уверены
   - Проект будет удален безвозвратно

### Пример использования

```bash
$ ./cleanup.sh
════════════════════════════════════════════════════════════
  🧹 ПОЛНАЯ ОЧИСТКА ПРОЕКТА
════════════════════════════════════════════════════════════

Создать резервную копию проекта? (y/n): y
Создаю архив...
✓ Резервная копия создана: /Users/.../diploma-backup-20260118-030000.tar.gz

Остановить Docker контейнеры? (y/n): y
Останавливаю контейнеры...
✓ Контейнеры остановлены

Удалить Docker образы (~500-600 MB)? (y/n): y
Удаляю Docker образы...
✓ Docker образы удалены

Очистить временные файлы и кэш? (y/n): y
Очищаю Python кэш...
✓ Временные файлы удалены

⚠️  ВНИМАНИЕ: Следующий шаг удалит ВСЕ файлы проекта!
Удалить весь проект полностью? (y/n): n

════════════════════════════════════════════════════════════
  ✅ ОЧИСТКА ЗАВЕРШЕНА
════════════════════════════════════════════════════════════
```

---

## Ручная очистка

### Шаг 1: Остановка Docker контейнеров

```bash
cd /Users/kirillzaitsev/Documents/diplom/diploma-project
docker-compose down -v
```

Эта команда:
- Останавливает все контейнеры
- Удаляет контейнеры
- Удаляет volumes (база данных)

### Шаг 2: Удаление Docker образов

```bash
docker rmi diploma-project-ml-service
docker rmi diploma-project-web-service
```

Освобождает ~500-600 MB.

**Проверка:**
```bash
docker images | grep diploma
# Должно быть пусто
```

### Шаг 3: Очистка временных файлов

```bash
cd /Users/kirillzaitsev/Documents/diplom/diploma-project

# Очистка через Makefile
make clean

# Или вручную:
find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
find . -type f -name "*.pyc" -delete
find . -type f -name "*.pyo" -delete
find . -name ".DS_Store" -delete
rm -rf ml-service/venv
rm -f data/results.db data/model.pkl
```

### Шаг 4: Полное удаление проекта

```bash
cd /Users/kirillzaitsev/Documents
rm -rf diplom
```

⚠️ **ВНИМАНИЕ**: Эта команда удалит все безвозвратно!

---

## Частичная очистка

Если хотите сохранить проект, но освободить место:

### Вариант 1: Легкая очистка (скрипт)

```bash
./cleanup-light.sh
```

### Вариант 2: Ручная частичная очистка

```bash
cd diploma-project

# Остановить Docker
docker-compose down -v

# Удалить временные файлы
make clean

# Удалить venv
rm -rf ml-service/venv

# Удалить базу данных
rm -f data/results.db
```

**Результат:**
- Освобождается: ~500-600 MB
- Сохраняется: Исходный код, документация, датасет
- Модель остается (можно пересоздать командой `python train_simple.py`)

---

## Создание резервной копии

### Автоматическое создание

Скрипт `cleanup.sh` предложит создать архив автоматически.

### Ручное создание

**Полный архив проекта:**
```bash
cd /Users/kirillzaitsev/Documents
tar -czf ~/Desktop/diploma-backup-$(date +%Y%m%d).tar.gz diplom/
```

**Только документация:**
```bash
cd /Users/kirillzaitsev/Documents/diplom/diploma-project
tar -czf ~/Desktop/diploma-docs-$(date +%Y%m%d).tar.gz \
    README.md \
    PRESENTATION.md \
    CHANGELOG.md \
    QUICK_START.md \
    docs/
```

**Только код (без зависимостей):**
```bash
tar -czf ~/Desktop/diploma-code-$(date +%Y%m%d).tar.gz \
    --exclude='venv' \
    --exclude='__pycache__' \
    --exclude='*.pyc' \
    --exclude='node_modules' \
    diplom/
```

### Восстановление из архива

```bash
cd ~/Desktop
tar -xzf diploma-backup-20260118.tar.gz -C /Users/kirillzaitsev/Documents/
```

---

## Проверка результатов

### После частичной очистки

```bash
# Проект должен существовать
ls /Users/kirillzaitsev/Documents/diplom
# → diploma-project

# Docker контейнеры удалены
docker ps -a | grep diploma
# → (пусто)

# Docker образы удалены
docker images | grep diploma
# → (пусто)

# Временные файлы удалены
find diploma-project -name "__pycache__"
# → (пусто)

find diploma-project -name "*.pyc"
# → (пусто)
```

### После полной очистки

```bash
# Проект удален
ls /Users/kirillzaitsev/Documents/diplom
# → ls: diplom: No such file or directory

# Docker ресурсы очищены
docker ps -a | grep diploma
# → (пусто)

docker images | grep diploma
# → (пусто)

# Порты освобождены
lsof -i :8080
# → (пусто)

lsof -i :8000
# → (пусто)

# Проверка места на диске
df -h /
# → Проверьте столбец "Available" - должно стать больше ~1-1.2 GB
```

---

## FAQ

### В: Можно ли восстановить проект после полного удаления?

**О:** Да, если вы создали резервную копию:
```bash
tar -xzf ~/Desktop/diploma-backup-*.tar.gz -C /Users/kirillzaitsev/Documents/
```

Без резервной копии восстановить невозможно.

---

### В: Что делать если cleanup.sh не запускается?

**О:** Добавьте права на выполнение:
```bash
chmod +x cleanup.sh
./cleanup.sh
```

---

### В: Docker образы не удаляются

**О:** Проверьте что контейнеры остановлены:
```bash
docker ps -a | grep diploma
docker-compose down -v
docker rmi -f diploma-project-ml-service diploma-project-web-service
```

---

### В: Сколько времени занимает очистка?

**О:**
- Легкая очистка: 10-30 секунд
- Полная очистка: 1-2 минуты
- Создание резервной копии: 30-60 секунд

---

### В: Можно ли очистить только Docker, оставив код?

**О:** Да, используйте `cleanup-light.sh` или выполните:
```bash
docker-compose down -v
docker rmi diploma-project-ml-service diploma-project-web-service
```

---

### В: Где найти созданную резервную копию?

**О:** На рабочем столе (`~/Desktop/`) с именем вида:
```
diploma-backup-20260118-030000.tar.gz
```

---

### В: Что делать если удалил проект случайно?

**О:**
1. Проверьте резервную копию на рабочем столе
2. Проверьте корзину (Trash)
3. Используйте Time Machine (если настроен)

Без бэкапа восстановить невозможно.

---

### В: Нужно ли удалять проект после защиты?

**О:** Не обязательно. Рекомендации:
- **Удалить**, если: диск заполнен, проект больше не нужен
- **Оставить**, если: планируете показывать работодателям, развивать проект
- **Архивировать**, если: хотите освободить место, но сохранить на будущее

---

### В: Как удалить только обученную модель?

**О:**
```bash
rm -f diploma-project/data/model.pkl
# Освобождается ~10 MB
# Пересоздать: cd ml-service && python train_simple.py
```

---

## Быстрая справка команд

```bash
# Автоматическая полная очистка
./cleanup.sh

# Легкая очистка (код остается)
./cleanup-light.sh

# Только остановить Docker
docker-compose down -v

# Только удалить образы
docker rmi diploma-project-ml-service diploma-project-web-service

# Только очистить кэш
make clean

# Создать резервную копию
tar -czf ~/Desktop/backup.tar.gz diplom/

# Полное удаление (одной командой)
cd /Users/kirillzaitsev/Documents/diplom/diploma-project && \
docker-compose down -v && \
docker rmi diploma-project-ml-service diploma-project-web-service 2>/dev/null && \
cd .. && rm -rf diplom
```

---

**Автор:** Дипломный проект "Процент профессионализма"
**Дата:** 2026-01-18
**Версия:** 1.0
